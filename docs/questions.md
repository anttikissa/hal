# Questions for next session

1. Thinking block header currently says `Hal (Codex 5.3, thinking)` as requested. Should this instead use the active model dynamically (e.g. `Hal (Sonnet 4, thinking)`), or keep fixed text?
2. Tabline fallback level 3 currently uses compact numeric labels from tab titles. Do you want strict no-separator form exactly like `123456...` (only indices concatenated), or keep current compact labels when they still fit?
3. Bash long-command wrapping now uses `\` continuation lines. Should we always force a leading `bash -lc` style prefix for copy/paste clarity, or keep raw command text as-is?
