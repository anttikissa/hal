# Session questions

- Should rotated logs keep the compact style (`history2.asonl`) or switch to dotted numbering (`history.2.asonl`) for readability?
- Should `loadMeta()`/`meta` naming in code be renamed to `loadSessionInfo()`/`sessionInfo` now that the file is `session.ason`?
