# dependencies (bun install)
node_modules

# IntelliJ based IDEs (.run holds their run configurations)
.idea
.run/

# Claude Code local settings
.claude/

# Finder (MacOS) folder config
.DS_Store

examples/*
state/

# Local user config; install copies config-template.ason here.
/config.ason
auth.ason
hal.js

# Local deployment and provider environment overrides (may contain secrets).
.env*
